name: Publish a NuGet package
on:
  push:
    paths: 
      - 'src/LEGO.AsyncAPI.Readers/**'
      - ".github/workflows/release-package.yml"
      - '!**/*.md' 
  workflow_dispatch:

jobs:
  release:
    runs-on: ubuntu-latest
    name: Publish a NuGet package
    steps:
      - name: Checkout repository
        uses: actions/checkout@v1
        
      - name: Setup .NET Core @ Latest
        uses: actions/setup-dotnet@v1
        with:
          source-url: https://nuget.pkg.github.com/LEGO/index.json
        env:
          NUGET_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}   
      
      - name: Build LEGO.AsyncAPI project and pack NuGet package
        run: dotnet pack src/LEGO.AsyncAPI.Readers/LEGO.AsyncAPI.Readers.csproj -c Release -o out -p:PackageVersion=1.0.$GITHUB_RUN_ID.$GITHUB_RUN_NUMBER  -p:NuspecFile=src/LEGO.AsyncAPI.Readers/LEGO.AsyncAPI.Readers.nuspec

      - name: Push generated package to GitHub Packages registry
        run: dotnet nuget push out/*.nupkg --skip-duplicate -n --api-key ${{secrets.GITHUB_TOKEN}}
             